import { Box, Typography, Button, TextField, Paper } from "@mui/material";
import { UseSelector, UseDispatch } from "react-redux";

const ParameterBlockTemplate = (props) => {

    return (
        <Paper>

        </Paper>
    );
};


export default ParameterBlockTemplate;